<div class="post-info">
    <h2>{{post.title}}</h2>
    <p>{{post.content}}</p>
    <p>{{post.User.username}}</p>
    <ul class="comments">
        {{#each post.Comments as |comment| }}
            <li>{{comment.comment_text}} by {{comment.User.username}}</li>
        {{/each}}
    </ul>
</div>

